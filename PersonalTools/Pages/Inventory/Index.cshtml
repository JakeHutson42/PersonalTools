@page
@{
    ViewData["Title"] = "Steam Inventory Lookup";
}

<div class="container px-3 px-lg-4">
    <div class="section-spacing">
        <h1 class="page-title">Steam Inventory Lookup</h1>
        <p class="page-subtitle">
            This page will eventually look up Steam inventory items.
        </p>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-4">
            <p class="mb-0 small-muted">
                Coming soon: Steam ID input, inventory lookup, item cards and filtering.
            </p>
        </div>
    </div>
</div>